git commit -m "Intitial commit"
git config --global user.name " sagar"
git config --global user.email "sagar.com"
touch .gitignore
git init
git add

git remote add origin git@github.com:sagarneupane/IrisFlower.git



ssh-keygen -t ed25519 -C "your_email@example.com"    --> for generating SSH KEY for github in windowss